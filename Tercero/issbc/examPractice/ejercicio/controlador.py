import modelo as model


def calcularArea(base, altura, lado, radio, self):
    return model.calcularArea(base, altura, lado, radio, self)


def calcularPerimetro(base, altura, lado, radio, self):
    return model.calcularPerimetro(base, altura, lado, radio, self)


def ordenarMayorArea(base, altura, lado, radio, self):
    model.ordenarMayorArea(base, altura, lado, radio, self)


def addFiguraLista(base, altura, lado, radio, self):
    model.addFiguraLista(base, altura, lado, radio, self)
